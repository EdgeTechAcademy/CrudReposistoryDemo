# connection string for my MySQL database.
# We are going to be using edgetech for this application
# see that useSSL at the end of the line? that turns off the Warnings
spring.datasource.url= jdbc:mysql://localhost:3306/edgetech?useSSL=false
spring.datasource.username=root
spring.datasource.password=password

#spring.jpa.hibernate.ddl-auto=create